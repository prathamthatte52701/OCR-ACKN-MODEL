# Backend API base URL. Leave unset for local dev - the Vite proxy
# forwards /api to http://localhost:8000. Set this on Vercel to the deployed
# backend's URL (e.g. https://your-backend.onrender.com/api).
VITE_API_URL=
