# Set this at deploy time (e.g. Vercel project env vars) to the deployed
# backend's URL, including the /api prefix. Left unset locally - dev uses
# vite.config.js's proxy to localhost:5002 instead.
VITE_API_URL=https://your-backend.onrender.com/api
